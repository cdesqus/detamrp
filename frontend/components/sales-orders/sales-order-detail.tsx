'use client';
import { useEffect, useMemo, useState } from 'react';
import { useRouter } from 'next/navigation';
import { MaterialRequirements, MaterialRequirement, RequirementResult } from './material-requirements';
import { formatMoney, formatQuantity } from '../../lib/number-format';
import { useToast } from '../toast/toast-provider';
type Line={id:string;itemCode:string;name:string;unit:string;quantity:string;deliveredQuantity:string;remainingQuantity:string;salesPrice:string;currency:string};
type Order={id:string;number:string;customerName:string;status:string;orderDate:string;lines:Line[]};
type Calculation={lineId:string;result:RequirementResult};
function combineRequirements(calculations:Calculation[]):RequirementResult{const materials=new Map<string,MaterialRequirement>(),totals:Record<string,number>={};for(const calculation of calculations){for(const material of calculation.result.materials){const current=materials.get(material.itemId);if(current){current.quantity=String(Number(current.quantity)+Number(material.quantity));current.kanbanEquivalent=String(Number(current.kanbanEquivalent)+Number(material.kanbanEquivalent));current.purchaseKanban=String(Number(current.purchaseKanban)+Number(material.purchaseKanban));if(material.value!==undefined)current.value=String(Number(current.value??0)+Number(material.value))}else materials.set(material.itemId,{...material})}for(const [currency,total] of Object.entries(calculation.result.totals??{}))totals[currency]=(totals[currency]??0)+Number(total)}return{nodes:[],materials:[...materials.values()],totals:Object.fromEntries(Object.entries(totals).map(([currency,total])=>[currency,String(total)])),costComplete:calculations.every(item=>item.result.costComplete!==false)}}
export function SalesOrderDetail({id}:{id:string}){const router=useRouter(),{showSuccess}=useToast();const[order,setOrder]=useState<Order|null>(null),[calculation,setCalculation]=useState<Calculation[]>([]),[error,setError]=useState(''),[submitting,setSubmitting]=useState(false);useEffect(()=>{fetch(`/api/sales-orders/${id}`,{credentials:'include'}).then(response=>response.ok?response.json():Promise.reject()).then((item:Order)=>{setOrder(item);if(item.status==='SUBMITTED')return fetch(`/api/sales-orders/${id}/requirements`,{credentials:'include'}).then(response=>response.ok?response.json():Promise.reject()).then(data=>setCalculation(data.lines??[]))}).catch(()=>setError('Sales order could not be loaded'))},[id]);const combined=useMemo(()=>combineRequirements(calculation),[calculation]);async function submit(){setSubmitting(true);setError('');try{const response=await fetch(`/api/sales-orders/${id}/submit`,{method:'POST',credentials:'include'});if(!response.ok){const body=await response.json();setError(body.message??'Sales order could not be submitted');return}const item=await response.json();setOrder(item);showSuccess('Sales order submitted successfully. BOM requirements are now available.');const requirements=await fetch(`/api/sales-orders/${id}/requirements`,{credentials:'include'});if(requirements.ok){const data=await requirements.json();setCalculation(data.lines??[])}}catch{setError('Sales order could not be submitted')}finally{setSubmitting(false)}}async function remove(){if(!window.confirm('Delete this draft sales order? This cannot be undone.'))return;setError('');try{const response=await fetch(`/api/sales-orders/${id}`,{method:'DELETE',credentials:'include'});if(!response.ok){const body=await response.json().catch(()=>({}));setError(body.message??'Sales order could not be deleted');return}showSuccess('Sales order deleted successfully.');router.push('/sales-orders')}catch{setError('Sales order could not be deleted')}}if(error&&!order)return <section className="module-index"><div className="table-empty"><strong>Could not load sales order</strong><span>{error}</span></div></section>;if(!order)return <section className="module-index"><p className="muted">Loading sales order...</p></section>;return <section className="module-index"><div className="page-title-row"><div><h1>{order.number}</h1><p className="muted">{order.customerName} · {order.status}</p></div><div className="toolbar-actions"><button onClick={()=>router.push('/sales-orders')}>Back to orders</button>{order.status==='DRAFT'&&<><button className="primary-button" disabled={submitting} onClick={submit}>{submitting?'Submitting...':'Submit sales order'}</button><button className="table-action" onClick={remove}>Delete draft</button></>}{order.status==='SUBMITTED'&&<button className="primary-button" onClick={()=>router.push(`/sales-orders/${id}/delivery`)}>Create delivery</button>}</div></div>{error&&<p className="form-error" role="alert">{error}</p>}<div className="table-frame"><table><thead><tr><th>Finished Good</th><th>Ordered</th><th>Delivered</th><th>Remaining</th><th>Unit</th><th>Sales Price</th><th>Currency</th></tr></thead><tbody>{order.lines.map(line=><tr key={line.id}><td>{line.itemCode} — {line.name}</td><td>{formatQuantity(line.quantity)}</td><td>{formatQuantity(line.deliveredQuantity)}</td><td>{formatQuantity(line.remainingQuantity)}</td><td>{line.unit}</td><td>{formatMoney(line.salesPrice,line.currency)}</td><td>{line.currency}</td></tr>)}</tbody></table></div>{order.status==='DRAFT'?<p className="muted">Submit this order to freeze its BOM calculation and show combined raw-material requirements.</p>:<MaterialRequirements result={combined} showCosts showBreakdown={false}/>}</section>}
