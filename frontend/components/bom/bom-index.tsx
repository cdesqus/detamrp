'use client';
import {Fragment,useCallback,useEffect,useState} from 'react';
type BOM={id:string;output:{itemCode:string;name:string;unit:string};revision:number;status:string;components:{itemCode:string;name:string;unit:string;usageQty:string}[]};
export function BOMIndex(){const[items,setItems]=useState<BOM[]>([]),[expanded,setExpanded]=useState<Set<string>>(new Set()),[error,setError]=useState(''),[activating,setActivating]=useState<string|null>(null);
 const load=useCallback(()=>{setError('');return fetch('/api/boms',{credentials:'include'}).then(x=>x.ok?x.json():Promise.reject()).then(x=>setItems(x.items??[])).catch(()=>setError('BOMs could not be loaded'))},[]);useEffect(()=>{void load()},[load]);
 async function activate(id:string){setActivating(id);setError('');try{const response=await fetch(`/api/boms/${id}/activate`,{method:'POST',credentials:'include'});if(!response.ok){let message='BOM could not be activated';try{const body=await response.json();message=body.message??body.error??message}catch{}throw new Error(message)}await load()}catch(e){setError(e instanceof Error?e.message:'BOM could not be activated')}finally{setActivating(null)}}
 function toggle(id:string){setExpanded(current=>{const next=new Set(current);if(next.has(id))next.delete(id);else next.add(id);return next})}
 return <section className="module-index"><div className="page-title-row"><div><h1>Bill of Materials</h1><p className="muted">Component quantities required to make one unit of an item.</p></div><a className="primary-button" href="/boms/new">New BOM</a></div>{error&&<p className="form-error" role="alert">{error}</p>}<div className="table-frame"><table><thead><tr><th>Output Item Code</th><th>Output Item</th><th>Revision</th><th>Status</th><th>Components</th><th>Actions</th></tr></thead><tbody>{items.length===0?<tr><td colSpan={6}><div className="table-empty"><strong>No BOMs yet</strong><span>Create a BOM to define the required components.</span></div></td></tr>:items.map(item=><Fragment key={item.id}><tr><td>{item.components.length>0&&<button className="table-action" aria-label={`${expanded.has(item.id)?'Collapse':'Expand'} ${item.output.itemCode}`} onClick={()=>toggle(item.id)}>{expanded.has(item.id)?'⌄':'›'}</button>} {item.output.itemCode}</td><td>{item.output.name}</td><td>R{item.revision}</td><td>{item.status}</td><td>{item.components.length}</td><td>{item.status==='DRAFT'&&<button className="primary-button" disabled={activating===item.id} onClick={()=>activate(item.id)}>{activating===item.id?'Activating...':'Activate'}</button>}</td></tr>{expanded.has(item.id)&&item.components.map(component=><tr key={`${item.id}-${component.itemCode}`}><td style={{paddingLeft:44}}>{component.itemCode}</td><td>{component.name}</td><td>—</td><td>Component</td><td>{component.usageQty} {component.unit} / {item.output.unit}</td><td/></tr>)}</Fragment>)}</tbody></table></div></section>}
