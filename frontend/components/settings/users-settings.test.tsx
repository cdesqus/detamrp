import{render,screen}from'@testing-library/react';import userEvent from'@testing-library/user-event';import{beforeEach,expect,it,vi}from'vitest';import{UsersSettings}from'./users-settings';
beforeEach(()=>vi.stubGlobal('fetch',vi.fn((input)=>{const url=String(input);const data=url.includes('/roles')?{items:[{id:'r1',code:'DIRECTOR',name:'Director',active:true}],total:1}:url.includes('approval-config')?{defaultApproverUserId:'',displayName:'',email:''}:{items:[],total:0};return Promise.resolve({ok:true,json:async()=>data})})));
it('opens the centered user editor with roles',async()=>{render(<UsersSettings/>);const user=userEvent.setup();await user.click(await screen.findByRole('button',{name:'New user'}));expect(screen.getByRole('dialog',{name:'New user'})).toBeInTheDocument();expect(screen.getByLabelText('Director')).toBeInTheDocument()});
