import{render,screen}from'@testing-library/react';import userEvent from'@testing-library/user-event';import{beforeEach,expect,it,vi}from'vitest';import{UsersSettings}from'./users-settings';
const approver={id:'u1',username:'director',displayName:'Director',email:'director@example.com',active:true,isPurchaseOrderApprover:true,roles:[{id:'r1',code:'DIRECTOR',name:'Director',active:true}],permissions:['po.approve'],createdByName:'Admin'};
beforeEach(()=>vi.stubGlobal('fetch',vi.fn((input)=>{const url=String(input);const data=url.includes('/roles')?{items:[{id:'r1',code:'DIRECTOR',name:'Director',active:true,permissionCodes:['po.approve']}],total:1}:{items:[approver],total:1};return Promise.resolve({ok:true,json:async()=>data})})));
it('opens the centered user editor with approver control',async()=>{render(<UsersSettings/>);const user=userEvent.setup();await screen.findByText('PO Approver');await user.click(screen.getByRole('button',{name:'New user'}));expect(screen.getByRole('dialog',{name:'New user'})).toBeInTheDocument();expect(screen.getByLabelText('Director')).toBeInTheDocument();expect(screen.getByLabelText('Set as Purchase Order Approver')).toBeInTheDocument();expect(screen.queryByText('Approval Configuration')).not.toBeInTheDocument()});
it('shows the approver badge',async()=>{render(<UsersSettings/>);expect(await screen.findByText('PO Approver')).toBeInTheDocument();expect(screen.getByText('Manage user accounts, roles, passwords, and the Supplier Order approver.')).toBeInTheDocument()});
it('confirms before replacing the current approver',async()=>{vi.stubGlobal('confirm',vi.fn(()=>true));render(<UsersSettings/>);const user=userEvent.setup();await screen.findByText('PO Approver');await user.click(screen.getByRole('button',{name:'New user'}));await user.click(screen.getByLabelText('Director'));await user.click(screen.getByLabelText('Set as Purchase Order Approver'));expect(window.confirm).toHaveBeenCalledWith('Replace Director as PO Approver?')});
