import { AppShell } from '../../components/app-shell/app-shell';
import { RoutingIndex } from '../../components/production/routing-pages';
export default function Page() {
  return (
    <AppShell title="Routing & Operations">
      <RoutingIndex />
    </AppShell>
  );
}
