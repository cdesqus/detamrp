'use client';
import { AppShell } from '../../components/app-shell/app-shell';
import { MasterDataCrud } from '../../components/master-data-crud';
async function options(signal:AbortSignal){const [suppliers,units]=await Promise.all([fetch('/api/master-data/suppliers?active=true&limit=200',{credentials:'include',signal}).then(r=>r.json()),fetch('/api/master-data/measurements?active=true&limit=200',{credentials:'include',signal}).then(r=>r.json())]);return {supplierId:suppliers.items.map((x:{id:string;code:string;name:string})=>({value:x.id,label:`${x.code} — ${x.name}`})),baseUnitId:units.items.map((x:{id:string;code:string;name:string})=>({value:x.id,label:`${x.code} — ${x.name}`}))}}
export default function Page(){return <AppShell title="Raw Materials"><MasterDataCrud title="Raw Materials" description="Definisi material, supplier utama, base unit, harga standar, dan isi per Kanban." endpoint="/master-data/raw-materials" singular="raw material" searchPlaceholder="Search item code, Sage code, or material" onLoadFields={options} initial={{code:'',sageItemCode:'',name:'',supplierId:'',baseUnitId:'',qtyPerKanban:1,minimumStock:0,standardUnitPrice:0,description:'',active:true}} columns={[{key:'code',label:'Item Code'},{key:'name',label:'Raw Material'},{key:'supplierName',label:'Primary Supplier'},{key:'baseUnitCode',label:'Base Unit'},{key:'qtyPerKanban',label:'Qty / Kanban'},{key:'standardUnitPrice',label:'Unit Price'},{key:'currency',label:'Currency'},{key:'active',label:'Status',render:r=>r.active?'Active':'Inactive'},{key:'createdByName',label:'Created By'}]} fields={[{key:'code',label:'Item Code',required:true},{key:'sageItemCode',label:'Sage Item Code',required:true},{key:'name',label:'Raw Material Name',required:true},{key:'supplierId',label:'Primary Supplier',type:'select',required:true},{key:'baseUnitId',label:'Base Unit',type:'select',required:true},{key:'qtyPerKanban',label:'Qty per Kanban',type:'number',step:'0.000001',required:true},{key:'minimumStock',label:'Minimum Stock',type:'number',step:'0.000001',required:true},{key:'standardUnitPrice',label:'Standard Unit Price',type:'number',step:'0.000001',required:true},{key:'description',label:'Description',type:'textarea'},{key:'active',label:'Active',type:'checkbox'}]}/></AppShell>}
